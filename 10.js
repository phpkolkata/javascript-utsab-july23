"use strict";

let hasLicence = false;
const passTest = true;

// if (passTest) hasLicenced = true;
if (passTest) hasLicence = true;
if (hasLicence) console.log("you can drive car now");

const interface = "test";
const private = "test";
