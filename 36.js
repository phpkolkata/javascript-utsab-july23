// IIFE - Immediately Invoked Function Expression
// - A functin expression which runs only once and cannot be invoked ever again

// function statement example
(function () {
  console.log("hi");
})();

// arrow function example
(() => console.log("hi"))();
